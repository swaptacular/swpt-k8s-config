source:
  kind: ConfigMap
  name: shardconfig
  fieldPath: metadata.annotations.unmodifiedSuffix
targets:
  - select:
      name: postgres.cluster-name.credentials.postgresql.acid.zalan.do
    fieldPaths:
      - metadata.name
    options:
      delimiter: "."
      index: 6
  - select:
      name: standby.cluster-name.credentials.postgresql.acid.zalan.do
    fieldPaths:
      - metadata.name
    options:
      delimiter: "."
      index: 6
  - select:
      name: db-owner.cluster-name.credentials.postgresql.acid.zalan.do
    fieldPaths:
      - metadata.name
    options:
      delimiter: "."
      index: 6
