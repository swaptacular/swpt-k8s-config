source:
  kind: ConfigMap
  name: nfs-server-config
  fieldPath: metadata.annotations.kustomize_nfs_service_cluster_ip
targets:
  - select:
      kind: Service
      name: nfs-server
    fieldPaths:
      - spec.clusterIP
