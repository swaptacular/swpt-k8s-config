source:
  kind: ConfigMap
  name: stomp-config
  fieldPath: metadata.annotations.kustomize_stomp_server_port
targets:
  - select:
      kind: Service
      name: app-gateway
    fieldPaths:
      - spec.ports.[name=stomp].port
