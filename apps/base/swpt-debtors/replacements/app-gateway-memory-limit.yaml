source:
  kind: ConfigMap
  name: nginx-gateway-config
  fieldPath: metadata.annotations.kustomize_app_gateway_memory_limit
targets:
  - select:
      kind: Deployment
      name: app-gateway
    fieldPaths:
      - spec.template.spec.containers.[name=app-gateway].resources.limits.memory
    options:
      create: true

