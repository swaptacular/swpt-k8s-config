apiVersion: apps/v1
kind: Deployment
metadata:
  name: trade-worker-turn-roller
  labels:
    app.kubernetes.io/component: turn-roller
spec:
  replicas: 1  # Must be 1, unless the shard is being split (then it must be 0)!
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/component: turn-roller
  template:
    metadata:
      labels:
        app.kubernetes.io/component: turn-roller
        log.pipeline: swpt-python-logging
    spec:
      priorityClassName: shard-background-worker
      imagePullSecrets:
        - name: regcreds
      containers:
        - name: turn-roller
          image: swpt-trade
          args: ['worker']
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
          envFrom:
            - secretRef:
                name: db-owner-credentials
              prefix: pg_
            - secretRef:
                name: broker-default-user
              prefix: rmq_
            - secretRef:
                name: internal-broker-default-user
              prefix: internal_rmq_
            - configMapRef:
                name: swpt-trade-config
            - configMapRef:
                name: trade-workerconfig
          env:
            - name: SOLVER_POSTGRES_URL
              value: "postgresql+psycopg://$(pg_username):$(pg_password)@trade-solver-db:5432/db?connect_timeout=10"
            - name: WORKER_POSTGRES_URL
              value: "postgresql+psycopg://$(pg_username):$(pg_password)@$(pg_cluster_name):5432/db?connect_timeout=10"
            - name: PROTOCOL_BROKER_URL
              value: "$(rmq_connection_string)"
            - name: PROTOCOL_BROKER_QUEUE
              value: "$(pg_cluster_name)"
            - name: PROTOCOL_BROKER_PROCESSES
              value: "0"
            - name: INTERNAL_BROKER_URL
              value: "$(internal_rmq_connection_string)"
            - name: INTERNAL_BROKER_QUEUE
              value: "$(pg_cluster_name)-internal"
            - name: INTERNAL_BROKER_PROCESSES
              value: "0"
            - name: FLUSH_PROCESSES
              value: "0"
            - name: HTTP_FETCH_PROCESSES
              value: "0"
            - name: TRIGGER_TRANSFERS_PROCESSES
              value: "0"
            - name: APP_LOG_LEVEL
              value: warning
            - name: APP_LOG_FORMAT
              value: json
      initContainers:
        - name: configure
          image: swpt-trade
          args: ['configure', 'worker']
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
          envFrom:
            - secretRef:
                name: db-owner-credentials
              prefix: pg_
            - secretRef:
                name: broker-default-user
              prefix: rmq_
            - secretRef:
                name: internal-broker-default-user
              prefix: internal_rmq_
            - configMapRef:
                name: swpt-trade-config
            - configMapRef:
                name: trade-workerconfig
          env:
            - name: SOLVER_POSTGRES_URL
              value: "postgresql+psycopg://$(pg_username):$(pg_password)@trade-solver-db:5432/db?connect_timeout=10"
            - name: WORKER_POSTGRES_URL
              value: "postgresql+psycopg://$(pg_username):$(pg_password)@$(pg_cluster_name):5432/db?connect_timeout=10"
            - name: PROTOCOL_BROKER_URL
              value: "$(rmq_connection_string)"
            - name: PROTOCOL_BROKER_QUEUE
              value: "$(pg_cluster_name)"
            - name: INTERNAL_BROKER_URL
              value: "$(internal_rmq_connection_string)"
            - name: INTERNAL_BROKER_QUEUE
              value: "$(pg_cluster_name)-internal"
            - name: SETUP_RABBITMQ_BINDINGS
              value: "yes"
            - name: APP_LOG_LEVEL
              value: warning
            - name: APP_LOG_FORMAT
              value: json
          resources:
            limits:
              cpu: "1"
              memory: 100Mi
            requests:
              cpu: 5m
              memory: 100Mi
        - name: await-migrations  # Awaits solver's database migrations.
          image: swpt-trade
          args: ["await_migrations"]
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
          envFrom:
            - secretRef:
                name: db-owner-credentials
              prefix: pg_
            - configMapRef:
                name: swpt-trade-config
            - configMapRef:
                name: trade-workerconfig
          env:
            - name: SOLVER_POSTGRES_URL
              value: "postgresql+psycopg://$(pg_username):$(pg_password)@trade-solver-db:5432/db?connect_timeout=10"
            - name: WORKER_POSTGRES_URL
              value: "postgresql+psycopg://$(pg_username):$(pg_password)@$(pg_cluster_name):5432/db?connect_timeout=10"
            - name: APP_LOG_LEVEL
              value: warning
            - name: APP_LOG_FORMAT
              value: json
          resources:
            limits:
              cpu: "1"
              memory: 100Mi
            requests:
              cpu: 5m
              memory: 100Mi
