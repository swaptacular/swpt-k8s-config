This file should contain a SOPS-encrypted cryptographic-grade secret.
For example, to generate this file you may use the following commands:

$ echo -n "$(openssl rand -base64 32)" > hydra-secret
$ sops encrypt hydra-secret > hydra-secret.encrypted
$ shred -z hydra-secret
$ rm hydra-secret
