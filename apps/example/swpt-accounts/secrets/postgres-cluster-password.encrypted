This file should contain the SOPS-encrypted password for the
"db_owner" user on all Postgres clusters which this application will
create. Once created, the password must not be changed. To generate
and SOPS-encrypt a random password, you may use these commands:

$ echo -n "$(openssl rand -hex 20)" > postgres-cluster-password
$ sops encrypt postgres-cluster-password > postgres-cluster-password.encrypted
$ shred -z postgres-cluster-password
$ rm postgres-cluster-password
