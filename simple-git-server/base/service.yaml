apiVersion: v1
kind: Service
metadata:
  name: git-server
spec:
  selector:
    app: simple-git-server
  ports:
    - name: ssh
      port: 2222
      protocol: TCP
      appProtocol: ssh
      targetPort: 22
  type: LoadBalancer
  externalTrafficPolicy: Local
