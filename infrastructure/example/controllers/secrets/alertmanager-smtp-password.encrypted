This file should contain the SOPS-encrypted password for the email
server that Alertmanager uses to send emails. To SOPS-encrypt your
password, you may use these commands:

$ echo -n 'YOUR_SECRET_PASSWORD' > alertmanager-smtp-password
$ sops encrypt alertmanager-smtp-password > alertmanager-smtp-password.encrypted
$ shred -z alertmanager-smtp-password
$ rm alertmanager-smtp-password
